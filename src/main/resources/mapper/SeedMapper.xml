<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.desheng.mapper.SeedMapper">

    <select id="searchByKeyword" resultType="com.desheng.model.Seed">
        SELECT * FROM seeds 
        WHERE LOWER(variety_name) LIKE LOWER(CONCAT('%', #{keyword}, '%')) 
           OR LOWER(approval_number) LIKE LOWER(CONCAT('%', #{keyword}, '%')) 
           OR LOWER(company) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
        ORDER BY approval_year DESC
    </select>

    <select id="findByMultipleFilters" resultType="com.desheng.model.Seed">
        SELECT * FROM seeds
        <where>
            <if test="cropType != null and cropType != ''">
                AND crop_type = #{cropType}
            </if>
            <if test="approvalRegion != null and approvalRegion != ''">
                AND approval_region LIKE CONCAT('%', #{approvalRegion}, '%')
            </if>
            <if test="startYear != null">
                AND approval_year >= #{startYear}
            </if>
            <if test="endYear != null">
                AND approval_year <= #{endYear}
            </if>
            <if test="company != null and company != ''">
                AND company LIKE CONCAT('%', #{company}, '%')
            </if>
        </where>
        ORDER BY approval_year DESC
    </select>

</mapper>